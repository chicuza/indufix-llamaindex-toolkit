# CLI Tools Requirements
# Install with: pip install -r requirements-cli.txt

# Rich terminal formatting (recommended but optional)
rich>=13.7.0

# HTTP requests
requests>=2.31.0

# YAML configuration
pyyaml>=6.0.1

# Existing deployment dependencies
# (from requirements.txt if not already installed)
langsmith>=0.1.0
